// pb_hooks/smtp_config.pb.js
// DISABLED - SMTP is now configured via /api/setup-smtp endpoint
// and email is sent via Brevo HTTP API directly

console.log("[smtp_config] SMTP config hook disabled - using Brevo HTTP API instead");
